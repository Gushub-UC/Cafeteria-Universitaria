import React from "react";

export default function Header(){
  return (
    <header style={{background:'#0f172a', color:'white', padding:'28px 20px'}}>
      <div style={{maxWidth:1100, margin:'0 auto'}}>
        <h1 style={{margin:0}}>Cafetería UDC — Plaza de Servicios</h1>
        <p style={{margin:'6px 0 0 0', opacity:0.9}}>Pedido prototipo — landing page</p>
      </div>
    </header>
  )
}
